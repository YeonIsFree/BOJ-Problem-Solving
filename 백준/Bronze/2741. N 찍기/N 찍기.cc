#include <bits/stdc++.h>
using namespace std;

int n;
int ans;

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> n;
    
    while(n--) {
        ans++;
        cout << ans << '\n';
    }
}